import { RootConfigSchema } from './types';
/**
 * Создает валидированную конфигурацию для NestJS с полной валидацией
 */
export declare const createNestJSConfiguration: (rootSchema: RootConfigSchema) => any;
